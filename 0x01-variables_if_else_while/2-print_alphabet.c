#include <stdio.h>

/**
 * main - this is the main func
 * Return: the return value is zero
 */

int main(void)
{
	char n;

	for (n = 'a' ; n <= 'z' ; n++)
		putchar(n);
	putchar('\n');
	return (0);
}

