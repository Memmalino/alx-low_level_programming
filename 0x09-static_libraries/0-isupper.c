#include "main.h"

/**
 * _isupper - function that checked x
 * @x: the argument to be checked
 * Return: 1 or 0
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}

